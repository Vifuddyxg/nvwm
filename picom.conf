backend = "glx";
vsync = true;
detect-rounded-corners = true;

# Keep regular fade for opacity/open-close transitions.
fading = true;
animations = true;

# Window open/close animations only.
# Do not add geometry/layout animations here, otherwise all tiled windows
# will move when NVWM rearranges the layout after a window appears/disappears.
animation-stiffness-in-tag = 120;
animation-stiffness-tag-change = 90.0;
animation-window-mass = 0.35;
animation-dampening = 18;
animation-clamping = true;
animation-for-open-window = "zoom";
animation-for-unmap-window = "squeeze";
animation-for-transient-window = "slide-up";

# Workspace change animations only.
animation-for-prev-tag = "slide-out-center";
enable-fading-prev-tag = false;
animation-for-next-tag = "slide-in-center";
enable-fading-next-tag = false;

# Leave geometry/layout animations undefined.
# If you ever add custom `animations = ({ ... })` rules, avoid triggers like:
# "geometry", "size", "position", "increase-size", "decrease-size".

active-opacity = 1.0;
inactive-opacity = 0.92;
frame-opacity = 0.90;
inactive-opacity-override = false;

shadow = true;
shadow-radius = 12;
shadow-opacity = 0.30;
shadow-offset-x = -10;
shadow-offset-y = -10;

blur-method = "gaussian";
blur-size = 10;
blur-deviation = 5.0;
blur-background = true;
blur-background-frame = true;
blur-background-fixed = false;

corner-radius = 10;
round-borders = 1;

shadow-exclude = [
  "class_g = 'Rofi'",
  "class_g = 'dmenu'",
  "name = 'Notification'",
  "_GTK_FRAME_EXTENTS@:c"
];

blur-background-exclude = [
  "window_type = 'dock'",
  "window_type = 'desktop'",
  "_GTK_FRAME_EXTENTS@:c"
];

rounded-corners-exclude = [
  "window_type = 'dock'",
  "window_type = 'desktop'"
];
